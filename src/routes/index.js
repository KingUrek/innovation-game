const routes = {
  home: '/home',
  nerd: '/nerd',
  login: '/login',
  register: '/register',
  perfil: '/perfil',
  challenge: '/desafio',
  checkout: '/finalizar',
  payment: '/pagamento'
};

const endpoints = {};

export default routes;
